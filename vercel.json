{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@now/node-server" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/server.js" }
  ],
  "env": {
    "NODE_OPTIONS": "--openssl-legacy-provider"
  }
}
